vim.filetype.add {
    extension = {
        -- Not longer needed but keeping this as an example
        -- astro = "astro",
    },
    filename = {
        ["Brewfile"] = "conf",
    },
    -- pattern = {},
}
