- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#  ~/.tmux.conf: tmux.conf
#  ~/.vim: vim
    ~/.zshrc: zshrc

- create:
    - ~/Developerino
   #- ~/.vim/undo-history

#- shell:
   #- [git submodule update --init --recursive, Installing submodules]
