- defaults:
    link:
      relink: true

- clean: ['~']

- link:
   ~/.zshrc: 
   ~/.gitconfig:
#  ~/.tmux.conf: tmux.conf
#  ~/.vim: vim

- create:
    - ~/Developerino
   #- ~/.vim/undo-history

- shell:
   - command: ./setup_homebrew.zsh
     stdout: true
     stderr: true
   # - [git submodule update --init --recursive, Installing submodules]
