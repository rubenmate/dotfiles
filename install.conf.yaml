- defaults:
    link:
      relink: true

- clean: ['~']

- link:
   ~/.zshrc: 
   ~/.gitconfig:
#  ~/.tmux.conf: tmux.conf
#  ~/.vim: vim

- create:
    - ~/Developerino
   #- ~/.vim/undo-history

#- shell:
   #- [git submodule update --init --recursive, Installing submodules]
